package dsa.algo.recursionAndBacktracking;

public class LearnBacktracking {

    public static void main(String[] args) {

        System.out.println("!!! Learn Backtracking !!!");


    }

    /*
    // Concept of backtracking:
    static boolean findSolution(parameter n) {
        if (solutionFound) {
            displaySolution();
            return true;
        }

        for (value: first_to_last) {
            if (isValid(value, n)) {
                applyValue(value);// saving solution till it is working
                if (findSolution(n))
                    return true;
                removeValue(value);// backtracking
                return false;
            }
        }
    }
    */


}
